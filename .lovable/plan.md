Zip the compiled GPT review history deliverables into a single downloadable file.

1. Locate the two existing review-history files (`COPY-REVIEW-HISTORY.html` and `gpt-review-history-rundown.md`) in `/mnt/documents/`.
2. Create a zip archive named `compiled-ai-reviews.zip` containing both files.
3. Present the zip as an artifact for download.
4. Delete the unrelated `seo-rise-up-codebase.zip` created in the previous turn so the correct file is clearly the one to grab.